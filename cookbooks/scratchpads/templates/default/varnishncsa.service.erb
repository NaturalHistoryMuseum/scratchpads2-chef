[Unit]
Description=Varnish HTTP accelerator logging

[Service]
Type=forking
Restart=always
ExecStart=/usr/bin/varnishncsa -n <%= node['varnish']['instance'] %> -a -w /var/log/varnish/varnishncsa.log -D

[Install]
WantedBy=multi-user.target